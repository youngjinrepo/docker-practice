name: Docker Image CI


on:
  push:
    branches: [ "main" ]


jobs:
  build-and-push:
    runs-on: ubuntu-latest  # 깃허브가 제공하는 임시 컴퓨터(우분투)를 씁니다.

    steps:
    # 1. 깃허브에 올린 내 코드를 내려받는다
    - name: Checkout code
      uses: actions/checkout@v3

    # 2. Docker Hub에 로그인한다 (아까 등록한 비밀번호 사용)
    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    # 3. 도커 이미지를 빌드하고 푸시한다
    - name: Build and push
      uses: docker/build-push-action@v4
      with:
        context: .
        push: true
        # 👇 [중요] 여기에 본인 Docker Hub 이미지 주소를 적으세요!
        tags: awdadawdargrg/my-flask-app:latest